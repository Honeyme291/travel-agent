import React from 'react';
import { OverlayProps } from '@uiw/react-amap-map';
export * from './useTileLayer';
export declare enum TileLayerType {
    SATELLITE = "satellite",
    TRAFFIC = "traffic",
    ROADNET = "roadnet"
}
export interface TileLayerProps extends OverlayProps, AMap.TileLayerOptions {
    /** @deprecated use {@link visible} */
    visiable?: boolean;
    /** 覆盖物是否可见 */
    visible?: boolean;
    type?: TileLayerType;
    options?: AMap.TileLayerOptions;
    /** 图层添加到地图上后的回调 */
    onAdded?: () => void;
    /** 图层从地图上移除后的回调 */
    onRemoved?: () => void;
}
export declare const TileLayer: React.ForwardRefExoticComponent<TileLayerProps & React.RefAttributes<TileLayerProps>>;
